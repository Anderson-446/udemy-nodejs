<div id ="book-page">
    <div class="container">
        <h1>{{book.title}}</h1>
        <p>Este livro tem {{book.num_p}} páginas.</p>
        <div class="actions">
            <a href="/books/edit/{{book.books_id}}"> Editar</a>
            <form action="/books/remove/{{book.books_id}}" method="POST">
            <input type="submit" value="Remover">
            </form>
        </div>
    </div>
</div>